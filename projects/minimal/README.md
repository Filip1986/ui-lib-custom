# Minimal App (Secondary Entry Point Check)

This tiny app imports only `Button` from `ui-lib-custom/button` to validate secondary entry points and tree-shaking behavior.

## Run

```powershell
cd D:\Work\Personal\Github\ui-lib-custom
npm run build:minimal
```

