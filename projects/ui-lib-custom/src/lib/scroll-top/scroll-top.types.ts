/** Visual variant for the ScrollTop button. */
export type ScrollTopVariant = 'material' | 'bootstrap' | 'minimal';

/** Size of the ScrollTop button. */
export type ScrollTopSize = 'sm' | 'md' | 'lg';

/** Scroll target: the global window or the immediate parent element. */
export type ScrollTopTarget = 'window' | 'parent';

/** Native scroll-behavior value applied when scrolling back to top. */
export type ScrollTopBehavior = 'smooth' | 'auto';
