/** Default slider position as a percentage (0–100). */
export const IMAGE_COMPARE_DEFAULT_VALUE: number = 50;

/** Keyboard step for ArrowLeft / ArrowRight keys. */
export const IMAGE_COMPARE_KEYBOARD_STEP: number = 1;

/** Larger keyboard step for PageUp / PageDown keys. */
export const IMAGE_COMPARE_KEYBOARD_LARGE_STEP: number = 10;

/** Minimum allowed slider value. */
export const IMAGE_COMPARE_MIN: number = 0;

/** Maximum allowed slider value. */
export const IMAGE_COMPARE_MAX: number = 100;

/** Default accessible label for the slider handle. */
export const IMAGE_COMPARE_ARIA_LABEL: string = 'Image comparison slider';
