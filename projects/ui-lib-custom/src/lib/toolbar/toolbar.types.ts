/** Visual design variant for the Toolbar component. */
export type ToolbarVariant = 'material' | 'bootstrap' | 'minimal';

/** Size modifier for the Toolbar component. */
export type ToolbarSize = 'sm' | 'md' | 'lg';
