/** Supported design variants for Tooltip. */
export type TooltipVariant = 'material' | 'bootstrap' | 'minimal';

/** Position of the tooltip panel relative to the host element. */
export type TooltipPosition = 'top' | 'bottom' | 'left' | 'right';

/** Which host events trigger tooltip visibility. */
export type TooltipEvent = 'hover' | 'focus' | 'both';
