/** Severity of the message - controls colour and icon. */
export type MessageSeverity = 'success' | 'info' | 'warn' | 'error' | 'secondary' | 'contrast';

/** Visual design variant - maps to the library theming system. */
export type MessageVariant = 'material' | 'bootstrap' | 'minimal';

/** Size token for the message component. */
export type MessageSize = 'sm' | 'md' | 'lg';
