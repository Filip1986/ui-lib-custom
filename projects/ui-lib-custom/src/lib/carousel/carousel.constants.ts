/** Default number of items visible in the viewport. */
export const CAROUSEL_DEFAULT_NUM_VISIBLE: number = 1;

/** Default number of items scrolled per navigation step. */
export const CAROUSEL_DEFAULT_NUM_SCROLL: number = 1;

/** Default orientation. */
export const CAROUSEL_DEFAULT_ORIENTATION: string = 'horizontal';

/** Viewport height used in vertical orientation when no override is provided. */
export const CAROUSEL_DEFAULT_VERTICAL_VIEWPORT_HEIGHT: string = '300px';

/** Minimum swipe distance (px) required to trigger page navigation on touch. */
export const CAROUSEL_SWIPE_THRESHOLD: number = 20;

/** Transition duration for the item-list slide animation (ms). */
export const CAROUSEL_TRANSITION_DURATION: number = 500;

/** ARIA label for the "previous" navigation button. */
export const CAROUSEL_ARIA_PREV_LABEL: string = 'Previous';

/** ARIA label for the "next" navigation button. */
export const CAROUSEL_ARIA_NEXT_LABEL: string = 'Next';

/** ARIA roledescription applied to each slide item. */
export const CAROUSEL_ARIA_SLIDE_ROLEDESCRIPTION: string = 'slide';
