/** ARIA label for the preview button indicator. */
export const IMAGE_ARIA_PREVIEW_LABEL: string = 'Preview image';

/** ARIA label for the close button inside the preview overlay. */
export const IMAGE_ARIA_CLOSE_LABEL: string = 'Close preview';

/** ARIA label for the zoom-in toolbar button. */
export const IMAGE_ARIA_ZOOM_IN_LABEL: string = 'Zoom in';

/** ARIA label for the zoom-out toolbar button. */
export const IMAGE_ARIA_ZOOM_OUT_LABEL: string = 'Zoom out';

/** ARIA label for the rotate-left toolbar button. */
export const IMAGE_ARIA_ROTATE_LEFT_LABEL: string = 'Rotate left';

/** ARIA label for the rotate-right toolbar button. */
export const IMAGE_ARIA_ROTATE_RIGHT_LABEL: string = 'Rotate right';

/** Step increment/decrement applied to the zoom scale per click. */
export const IMAGE_ZOOM_STEP: number = 0.1;

/** Minimum zoom scale. */
export const IMAGE_ZOOM_MIN: number = 0.1;

/** Maximum zoom scale. */
export const IMAGE_ZOOM_MAX: number = 5;

/** Degrees to rotate per rotate-left / rotate-right click. */
export const IMAGE_ROTATE_STEP: number = 90;
