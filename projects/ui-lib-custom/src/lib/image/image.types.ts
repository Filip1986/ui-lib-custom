/** Design variant for the Image component. */
export type ImageVariant = 'material' | 'bootstrap' | 'minimal';

/** Size token for the Image component. */
export type ImageSize = 'sm' | 'md' | 'lg';
