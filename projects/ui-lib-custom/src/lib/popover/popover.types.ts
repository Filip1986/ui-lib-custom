/** Supported design variants for Popover. */
export type PopoverVariant = 'material' | 'bootstrap' | 'minimal';

/** Vertical placement of the popover panel relative to the target element. */
export type PopoverPlacement = 'above' | 'below';
